# Skifta — build roadmap & status

This file is the continuity record between work sessions (spec §14.2). It maps
**every** section of [`restaurang-app-spec-v2.md`](restaurang-app-spec-v2.md) to
its current build status so nothing is silently dropped. Update it in the same
commit as the code it describes.

**Legend:** ✅ done · 🟡 partial / scaffolded · ⬜ not started · 🚫 deliberately out of MVP scope (§17)

---

## Foundation (this session)

- ✅ Project scaffold — Next.js 16 (App Router) + TypeScript + Tailwind v4, private git repo.
- ✅ Design system — palette & type from the mockups (`src/app/globals.css`): cream surfaces, deep teal brand, dark sections, terracotta accent for deviations.
- ✅ i18n layer (§11) — `src/i18n/`. Swedish base + English, typed catalogs, `t()` helper, **no hardcoded UI text**. Landing toggle swaps locale **without reload**.
- ✅ Landing page (§11, §12.3) — `src/components/landing/`. Hero, features, stats band, pricing (Bas 249 / Fullt 499), CTA, footer. SV default, discreet SV/EN pill. Verified: production build passes + `/` prerenders SSR in Swedish.
- ✅ Data model (§9) — `prisma/schema.prisma`. All §9 entities + the Deviation lifecycle (§6.3). Append-only `ClockEvent` + `ClockEventAdjustment`; person-centric `User`/`Membership`. Validates & generates (Prisma 6).
- ⬜ Database provisioning + first migration (needs a Postgres `DATABASE_URL`).

## Auth & access spine (this session)

- ✅ Authorization policy + guard (§12.2) — single layer (`src/lib/authz.ts` + `guard.ts`), keyed on tier + role. **Negative tests pass** (Bas → denied economy/clock; employee → denied admin; co-owner = owner).
- ✅ Session — signed JWT in an httpOnly cookie (`src/lib/session.ts`, edge-safe verify in `session-token.ts`).
- ✅ Edge gate — `src/proxy.ts` bounces unauthenticated users off `/app`. Verified: `/app` → 307 `/login`.
- ✅ OTP login — request-code / verify / logout APIs, `VerificationCode` model, hashed codes, contact normalization to E.164. Console sender so the flow runs with no SMS keys. Verified: bad contact → 400.
- ✅ Login UI + restaurant picker (§3.3) — `/login` (contact → code) and `/app/select`.
- ✅ End-to-end login — DB provisioned (Neon Postgres), first migration run, real login→session→`/app` flow verified against it.
- ✅ Real SMS/email provider — superseded by the §4/§5 "Wire real SMS/email providers: 46elks + Resend" work; no longer console-only.
- 20 unit tests green (`npm test`); production build passes.
- 🔍 **Owner-reported bug, under investigation: "I always need to log in again."** The 30-day session cookie + the `/login` "already have a session → skip straight to `/app`" check are both correctly implemented in code (`session.ts`, `session-token.ts`, `app/login/page.tsx`) — this isn't an obvious code bug. Two real candidate causes, ranked by likelihood, **pending the owner confirming which applies**: (1) **most likely** — visiting a fresh per-deployment Vercel URL after each push (a new deployment gets its own unique host in addition to the stable production domain/alias) rather than the stable URL; the session cookie is host-scoped with no `domain` attribute, so it would silently not carry over between hosts, and this session alone pushed 4 deployments. (2) `AUTH_SECRET` not being a fixed, persistent value in Vercel's **Production** environment variables — it signs every session JWT *and* every clock-kiosk QR token (`clock-token.ts`), so if it ever changes, every existing session (and every already-printed QR code) invalidates at once. Needs the owner to check Vercel's dashboard (Settings → Environment Variables → confirm `AUTH_SECRET` is set once for Production and never edited) and confirm which URL they've been testing against.

## Invite system (this session, §4)

- ✅ Single invite — `POST /api/invites` creates a personal one-time link (random 24-byte token), sent via the console messaging stub. Owner/co-owner only (`members:manage`).
- ✅ Accept flow — `/join/[token]`: public preview (`GET /api/join/[token]`), OTP re-verification against the invite's own contact (never the requester's input), then `POST .../accept` creates the User (or reuses an existing one for multi-restaurant) + Membership and signs the person in.
- ✅ Single-use enforcement — token is dead after consumption (`CONSUMED` status), 404s on reuse. Forwarded links can't be completed by someone else (contact mismatch fails OTP).
- ✅ Revoke — `POST /api/invites/[id]/revoke`, owner/co-owner only, restaurant-scoped; revoked links 404 immediately.
- ✅ Bulk invite — `POST /api/invites/bulk` accepts a list of {name, contact, role} rows (admin UI: paste "Name, contact" per line); invalid rows are skipped and reported, valid ones get a personal link each.
- ✅ Admin UI — `/app/admin/members`: single-invite form, bulk-paste box, pending/consumed/revoked list with revoke action. Linked from the app home for owners/co-owners.
- ✅ Verified end-to-end against the real DB: created invite → code logged → accepted → new session → reuse blocked (404) → non-admin blocked from inviting (403) → bulk invite with one bad row → revoke → revoked link dead.
- ✅ CSV file upload — `src/lib/csv-invite.ts` (pure, browser-side + unit-tested) parses an uploaded CSV into `{name, contact, role}` rows, then reuses the same validated `/api/invites/bulk` endpoint as the paste box. Tolerant of what Swedish Excel emits on Save As → CSV: BOM, `;`/`,`/tab delimiters, quoted fields, an optional header row, and 2- or 3-column layouts (role optional). Admin UI (`MembersAdminView`) gains a file picker → parsed-rows preview → send. 9 unit tests. Binary `.xlsx` stays out (no zip/XML parser dep, consistent with §17/no-new-deps) — the owner exports to CSV first, surfaced in the UI hint.

## Shifts section (this session, §6.1)

- ✅ Week view — `/app/schedule` (`ScheduleView`), Mon–Sun columns, prev/next-week nav, shows everyone's shifts to owner/co-owner, own + open shifts to employees.
- ✅ Owner shift CRUD — create (assign or leave OPEN), delete; required tags auto-create by name (no separate tag-creation step needed when scheduling).
- ✅ Open shifts, both fill modes — `openShiftFill` (FIRST_COME / MANUAL_PICK) read from the restaurant's settings and **enforced server-side**, not just hidden in the UI: `/claim` only works in FIRST_COME, `/interest` + owner `/pick` only in MANUAL_PICK.
- ✅ Qualification matching (§7) — `qualifiedMembers()` gates claim/interest/swap-accept by competence tag; a shift with no required tags is open to everyone.
- ✅ "Can't work / sick" swap flow — `POST /api/shifts/[id]/swap` starts a request in the restaurant's default mode (`swapDefaultMode`); DIRECTED needs an owner `/direct` pick, BROAD is visible to all qualified colleagues; `/respond`, `/decline`, `/approve`, `/cancel` cover the rest of the lifecycle. Approval is what actually reassigns the shift.
- ✅ Availability — `/app/availability`, weekly recurring ranges, full replace on save (`PUT /api/availability`).
- ✅ Competence tags admin — `TagsAdmin` inside `/app/admin/members`: create/delete tags, per-member tag checkboxes.
- ✅ Verified end-to-end against the real DB with two test employees: assigned shift → sick button → owner directs to colleague → colleague accepts → owner approves → shift reassigned; open tagged shift → unqualified employee blocked → tag assigned → MANUAL_PICK interest → owner pick; employee correctly blocked (403) from shift:manage/tags:manage actions.
- ✅ Escalation cron — `escalateOverdueSwaps` (`swaps.ts`) flips a PENDING swap past its `escalateAt` window to `ESCALATED` and pings the owners; idempotent (only PENDING rows, so re-running never re-notifies). New `/api/cron/shifts` route (CRON_SECRET bearer, same pattern as billing) on a `*/15` Vercel Cron (`vercel.json`). ESCALATED is a visibility state, not a lock: reply/decline/direct/cancel all still act on it.
- ✅ Notifications (new shift, change, swap needs a reply) — `src/lib/notify.ts`, a best-effort layer over `messaging.ts` (real 46elks SMS / Resend email, now wired): assign → "nytt pass", time change → "ändrats", swap request → "behöver svar" (BROAD = all qualified, DIRECTED = the picked colleague), plus accepted→owner, approved→both parties, escalated→owner. A failed send never rolls back the schedule change (try/caught, swallowed). Swedish literals, consistent with `billing.ts` + SV-only MVP (§17).
- ✅ Double-booking prevention — `assertNoDoubleBooking` (`shifts.ts`) refuses to put a person on two overlapping shifts (half-open interval overlap, checked against `ShiftAssignment` rows), enforced server-side (throws 409) on create, update, first-come claim, manual pick, owner-assign, and swap approval — not just hidden in the UI.
- 🔧 Schema addition: `ShiftInterest` model (manual-pick interest list) — wasn't in the original §9 model set, added this session with its own migration.
- ✅ **Multi-slot shifts** (follow-up to the schema brief, this session) — a shift can now take several people (`Shift.slots`), not just one. `ShiftAssignment` (new join model) replaces the old single `Shift.assignedUserId`; a shift is `OPEN` while `assignments.length < slots`, `ASSIGNED` ("full") once they match. `assertNoDoubleBooking`, `createShift`, `updateShift`, `claimOpenShift`, `pickInterested`, swap accept/approve all moved onto the join table. New `assignToShift`/`unassignFromShift` (`POST /api/shifts/[id]/assign|unassign`) let the owner directly fill or free one slot. Concurrent fills are serialized with a `SELECT ... FOR UPDATE` row lock (`fillSlot` in `shifts.ts`) so two simultaneous claims/picks can never push the assignment count past `slots` — the multi-slot equivalent of the old single-assignee `updateMany` exclusivity trick. **Migration written, not yet applied to the live Neon DB** (backfills existing `assignedUserId` rows into `ShiftAssignment` before dropping the column — same "ask before touching the shared production DB" rule as the two prior pending migrations; run `npx prisma migrate deploy` at deploy time).
- ✅ **Auto-rejection of leftover interest, with notification** (the brief's flagged gap) — when a shift fills, `rejectRemainingInterests` (`shifts.ts`) sets every other still-`PENDING` `ShiftInterest` to `REJECTED` (new status field, not deleted) and notifies each rejected candidate (`notifyInterestRejected`) — replaces the old silent `deleteMany`.
- ✅ **Bulk/recurring shift creation** — `POST /api/shifts/bulk` (date range + weekday checkboxes, reuses `createShift` per matching date so double-booking + tag auto-create still apply; a bad date is skipped + reported, not a whole-batch failure). Pure date expansion (`expandWeekdayDates`, unit-tested) + a `BulkShiftForm` modal next to "Nytt pass" in `ScheduleView`. The existing AI scheduling entry is repositioned as a secondary "Testa skapa med AI" button, not replaced.
- ✅ **AI bulk-create timezone bug, fixed** — `schedule/ai/approve/route.ts` was building `new Date(\`${date}T${time}:00\`)`, which JS parses as UTC, silently shifting every AI-proposed time by the Stockholm offset. New `src/lib/local-time.ts` (`localDateTimeToUtc`, unit-tested for both CET/winter and CEST/summer) converts local wall-clock time to the correct UTC instant via `Intl.DateTimeFormat`, DST-aware, no dependency. Used by both the AI-approve route and the new bulk-create route.
- ✅ **Owner shift-card info** — `ScheduleView`'s `ShiftCard` now lists every assignee (not just one) with an "X/Y platser" count once `slots > 1`, an inline "×" to unassign, a "Tilldela fler …" dropdown of qualified members for any shift with an open slot, a "Behöver ny arbetare" pill when a previously-filled shift drops back open, and "Nekat av X" surfaced directly on the collapsed card (previously only visible inside the expanded swap block). Competence tags were already shown — confirmed visible at a glance, not just on expand.
- ✅ **Notification center** (in-app feed, additive to SMS/e-mail) — new `Notification` model (structured fields — `type`/`restaurantName`/`startsAt`/`count` — not pre-rendered text, so the feed renders through `t()` like every other in-app surface; SMS bodies keep their existing rule-#1 exception, this new in-app surface doesn't get one). Every existing `notify.ts` call site (`notifyShiftAssigned/Changed`, `notifyInterestRejected`, `notifySwapNeedsReply/Accepted/Approved/Escalated`, `notifyDeviationDigest`) now writes a matching `Notification` row alongside the SMS/e-mail send — additive, the SMS/e-mail path is unchanged. `GET /api/notifications` (mine, newest-first + unread count), `POST /api/notifications/[id]/read`. New `/app/notifications` feed page (`NotificationsView`) — actionable items (a swap needing a reply, an accepted swap awaiting approval) get "Acceptera"/"Godkänn" buttons that call the **existing** `/api/swaps/[id]/respond|approve` endpoints, never a new write path. Bell icon + polled unread badge (20s, same pattern as `onShiftNow()`) added to `ScheduleView`'s nav.

## Clock-in section (this session, §5/§6.2)

- ✅ Stamp endpoint — `POST /api/clock/stamp`, **public by design**: place is proven by the QR/kiosk token, identity by PIN or WebAuthn — no prior login. **Append-only** `ClockEvent`, **idempotent on `clientId`** (replay → `duplicate:true`, never a second row), direction auto-toggles from the last stamp. Verified end-to-end against the real DB.
- ✅ Place proof — stateless HMAC clock token (`src/lib/clock-token.ts`, no schema migration); tampered/forged tokens → 400. The QR encodes `/clock/<token>`.
- ✅ **Tier gate enforced server-side** — clock-in is FULL only; a Bas restaurant's kiosk token → **403 `tier_locked`** (the kiosk has no session/role, so the tier is checked on the restaurant directly). Proven end-to-end.
- ✅ PIN method (§5 method 2) — set/replace own PIN (`POST /api/clock/pin`), hashed with scrypt; kiosk PIN entry resolves to the member restaurant-scoped, all hashes checked to avoid an early-exit timing signal.
- ✅ WebAuthn method (§5 method 1) — `@simplewebauthn` v13. Registration while signed in (own device, `register/options`+`verify`); **discoverable** kiosk authentication (`auth/options`, verified inside the stamp). Many credentials per user (new/lost phone). Only the public credential is stored. **Built + typechecks + build passes; needs real-device + HTTPS field verification** (Face ID/Touch ID can't be exercised here).
- ✅ Tolerance window + graded deviations (§6.2) — `src/lib/deviations.ts`: `|Δ| ≤ low` = none, `≤ high` = LOW, else HIGH; **repeated same-direction pattern escalates to HIGH**. A stamp is matched to the day's assigned shift by closest boundary; an out-of-tolerance stamp opens an OPEN `Deviation` **assigned to an owner** (§6.3). Unit-tested + verified (90-min-late IN → HIGH/OPEN, owner-assigned).
- ✅ Own clock view — `/app/clock` (`ClockView`): week history + **accumulated hours** (paired IN→OUT, `accumulateHours`) + still-clocked-in indicator; device/PIN setup. No access to others' stamps or economy data (§6.2).
- ✅ Kiosk — `/clock/[token]` (`ClockKiosk`), public, outside the `/app` proxy: big tap-to-stamp (WebAuthn) + PIN pad, green check + haptic, invalid/tier-locked states. Verified it renders (200 + localized invalid message).
- ✅ Offline resilience (§6.2) — IndexedDB queue (`src/lib/clock-queue.ts`) + flush-on-reconnect (`useSyncExternalStore` online status) + service worker shell cache (`public/clock-sw.js`). PIN stamps queue when offline and replay idempotently; calm "we'll sync" message, never an error. (WebAuthn needs a live challenge, so offline stamping is the PIN path.)
- ✅ Admin setup — `/app/clock/setup` (`ClockAdminView`): printable **QR** (`qrcode`) + copyable kiosk link + configurable tolerance window (`POST /api/clock/settings`, settings:manage + FULL).
- ✅ UI aligned to the design references (`Design references for claude/`) — kiosk reworked to the two-column Scandinavian layout (PIN keypad + dot indicators, persistent "scan with your phone" QR/Face ID card, live **"on shift now"** list, big header clock); employee mobile view reworked to the teal **"on shift now"** hero (live elapsed time, in-app Face ID **clock-out** via `POST /api/clock/self`, period total, flagged history rows) + bottom tab bar. New shared primitives `Avatar`, `TagDot` (`tag-color.ts`), `MobileTabBar`. Both surfaces visually verified in the preview against the mockups.
- ✅ In-app self clock-in/out (`/api/clock/self`) — session identity + WebAuthn confirm; `onShiftNow()` realtime list (`/api/clock/onshift`, reused by §6.3).
- ✅ i18n — full `clock` catalog (sv source + en), tier-gated nav links. 40 tests green (20 new: clock-token, deviations, clock pure logic); production build passes.
- ✅ **Lost/new-device self-service re-registration** (§5 "förlorad enhet") — kiosk-public flow (no `/app` session, so a shared tablet never ends up signed into the owner/economy app): contact → `DEVICE_REREGISTER` OTP (`POST /api/clock/webauthn/reregister/request`) → on verify, the server resolves the user and immediately starts a WebAuthn registration ceremony, binding the userId to a short-lived single-use cookie rather than a full session (`.../reregister/verify`) → the browser completes the ceremony → `.../reregister/finish` persists the new credential. UI: a "Tappat telefonen?" link on the kiosk (`ClockKiosk.tsx`) opens an inline contact → code → Face ID/Touch ID panel, reusing the same `@simplewebauthn/browser` ceremony as normal registration.
- ✅ **Owner-initiated device reset** — `POST /api/members/[userId]/reset-device` (owner/co-owner, `members:manage`, both tiers): removes a colleague's WebAuthn credentials so they can self-register fresh ("ägare/co-owner kan återställa ... eftersom de redan är betrodda admins"). Admin UI: "Återställ enhet" button per person in `PeopleAdmin.tsx`. **Known edge case, by schema design**: `WebAuthnCredential` has no restaurant scope (one global biometric identity per person across every restaurant they work at, per the §9 data model), so resetting it at restaurant A also clears clock-in credentials at restaurant B if the person works both. Accepted rather than redesigning the schema for this; the person just re-registers (self-service or normal login), unaffected elsewhere.
- ⬜ Not live-exercised end-to-end against the real DB or in a browser this session (the preview tool hit an unrelated reload-loop instability) — verified via clean build/typecheck/lint and structural parity with the already-verified login OTP flow (`/api/auth/request-code` + `/verify`), which this mirrors call-for-call. Same gap as WebAuthn generally: needs a real device + HTTPS origin to fully confirm.
- ✅ **Confirmed: the kiosk *is* the "shared device, public link, printable QR" feature.** `/clock/[token]` is fully public (outside the `/app` auth proxy, no login needed by anyone using it) and `/app/clock/setup` already gives the owner a printable QR + copyable link. **Live-tested against the real DB this session**: generated a real token for an existing FULL-tier restaurant, hit `/clock/<token>` directly — 200, real restaurant name rendered, QR `<svg>` present, "Skanna med din telefon" scan card present; a forged/garbage token correctly renders the invalid-code state instead of leaking anything. The exact flow described (owner prints/hangs it, anyone can scan or tap on the shared screen, no personal Skifta login needed) is what's built — nothing new to add here.
- ⬜ <3s field validation under lunch rush (§5 process requirement) and full cold-start PWA offline boot (§14.3, SW currently caches the shell after first load) — need a real device/deploy.
- ✅ **Weekly deviation digest** — `src/lib/deviation-digest.ts`: `runDeviationDigest` folds into the existing daily billing cron (no second Vercel Cron schedule). Each restaurant tracks its own `deviationDigestSentAt` (additive migration `deviation_digest_tracking`, **written but not applied to the live Neon DB** — same "ask before touching the shared production DB" rule as the GDPR migration; run `npx prisma migrate deploy` at deploy time), so the digest fires on a rolling ~7-day cadence per restaurant rather than depending on the cron landing on an exact weekday — a missed cron window catches up instead of silently skipping a week. Owners get a real SMS/email ("X öppna avvikelser väntar på granskning") only when there's at least one open `Deviation`; the timestamp still advances either way so a quiet week doesn't notify. 4 new unit tests on the pure due-date logic; **114 tests green**.
- ✅ **Bug fix: "vi känner inte igen dig" on mobile QR/PIN, root-caused** — `ClockKiosk.tsx` was collapsing *every* `/api/clock/stamp` error other than `tier_locked` into the generic "we don't recognise you" message, including `invalid_token` (a stale/forged place-token — e.g. printed before an `AUTH_SECRET` rotation, the same root cause already on record above for "always logged out") and `not_a_member`. Each error code now maps to its own message (`stampErrorKey`): a re-print-the-QR message for `invalid_token`, a distinct one for `not_a_member`, the original wording only for an actual wrong PIN/unregistered device. Most real-world reports of this bug are the place-token being stale, not the identity check failing.
- ✅ **Mobile clock-IN now requires the venue QR; clock-OUT doesn't.** `ClockView.tsx`'s "Stämpla in" used to call `/api/clock/self` directly — session + Face ID, no place proof at all, contradicting the "no clock-in without the QR" requirement. It now opens `QrClockInOverlay`: `BarcodeDetector` (Android/Chrome) decodes the printed QR in-page and navigates straight to the kiosk URL it encodes (`/clock/<token>`), reusing the already-built/verified kiosk flow end-to-end (token check, Face ID/PIN, offline queue) instead of duplicating it; Safari/iPhone (no `BarcodeDetector`, and no new dependency per §17) gets clear instructions to scan the same QR with the phone's own camera app, which already opens that URL natively. `/clock/[token]` gained a best-effort session check (`hasSession`) so a signed-in worker who scans their way there gets a "Tillbaka till appen" link back to `/app/clock` after stamping, instead of being stranded on the public kiosk page. Clock-OUT is untouched — still `/api/clock/self`, no scan, reachable straight from `/app/clock`.

## Economy/admin section (this session, §6.3)

- ✅ Owner overview — `/app/economy` (`EconomyView`), FULL tier + owner/co-owner only (`economy:view`, verified: unauth → 307 `/login`, API → 401). Period-scoped (calendar month, prev/next nav). Four tabs: **Payroll period** (stat cards + per-employee table), **Time clock** (realtime on-shift), **Deviations** (review queue), **Settings** (export).
- ✅ Automatic hour summarisation — `src/lib/economy-data.ts` sums worked hours per employee per period by reusing the append-only `accumulateHours` pairing; constant number of queries regardless of headcount. Stat cards: total hours, flagged (open) deviations, on-shift count, activation/adoption (X of Y).
- ✅ Deviation review lifecycle (§6.3) — `POST /api/economy/deviations/[id]` with `review` (→ REVIEWED), `approve` ("godkänn som stämplat" → APPROVED), or `adjust`. **Adjust honours append-only**: it writes a `ClockEventAdjustment` pointing back at the original stamp + reason, never mutating the `ClockEvent`, then approves the deviation. Restaurant-scoped; `deviation:review` (FULL + admin).
- ✅ **Export gate enforced server-side** (§6.3 — "ogranskade avvikelser inkluderas ALDRIG tyst") — `splitExportable` holds back any member with an OPEN deviation; `GET /api/economy/export` returns **409 + the blocked names** unless the owner explicitly acknowledges exclusion (`?exclude=1`), which exports only the cleared members. No silent inclusion, no silent auto-adjustment. Unit-tested.
- ✅ Export formats + "save as my default" — Fortnox / Visma / generic CSV column layouts (`buildExportCsv`, documented plausible structures; CUSTOM falls back to generic until a template is imported). CSV with UTF-8 BOM so Excel reads å/ä/ö. `POST /api/economy/export/default` persists `defaultExportFormat` (settings:manage + FULL).
- ✅ "Exportera all min data" (§6.3 + §13 GDPR portability) — `GET /api/economy/export-all?format=json|csv`: full restaurant dump (employees, schedules, stamp history) as structured JSON or a stamp-history CSV. Gated on `members:manage` (admin, **both tiers**) so a Bas owner can also take their data.
- ✅ Realtime overview — reuses `onShiftNow()` ("vem är instämplad just nu").
- ✅ i18n — full `economy` catalog (sv source + en). Economy nav links added to schedule/clock desktop headers (admin) + owner mobile Admin tab → `/app/economy` (bar-chart icon, per mockup); member admin cross-linked. 12 new unit tests (period math, CSV escaping, export gate, format layouts); **52 tests green** (8 files); production build passes.
- ⬜ OB classification + estimated gross payroll (the mockup's "OB" and "Est gross" columns/cards) — deferred to **§8.2** (AI-assisted payroll draft): needs an OB rule set + per-member rate, which is the next milestone. Hours/deviations/export underlag is complete and honest without fabricated money figures.
- ✅ **Import-my-own-template (CUSTOM column mapping upload)** — `src/lib/export-template.ts` (pure, browser-side, mirrors `csv-invite.ts`'s BOM/`;`/quoting tolerance) reads just the header row of an uploaded sample/export file; the owner maps our 4 known fields (employee, period start/end, hours) onto those exact headers, each field usable at most once. `POST /api/economy/export/template` validates + saves it as the restaurant's CUSTOM `ExportTemplate` (replaces any prior one — no versioning needed here, unlike payroll rules, since an export isn't an approved/traced record). `buildExportCsv` (`economy.ts`) now takes the mapping and emits the owner's exact column order/labels for CUSTOM; with no template saved it still falls back to the honest generic layout. UI: a "Egen exportmall" upload + column-mapping picker under the CUSTOM format choice in `SettingsTab`. 16 new unit tests (header parsing, mapping validation, custom CSV output); **110 tests green**; build/lint clean.
- ✅ Weekly deviation digest notification (§6.3) — see the §6.2 entry above; `runDeviationDigest` covers both sections (the digest is keyed on `Deviation.status`, which §6.3's review queue is the UI for).

## AI payroll draft (this session, §8.2)

- ✅ **Deterministic rules engine** (`src/lib/payroll/engine.ts`) — the numbers are rule-based, not AI (§8.2). Pairs append-only stamps into worked intervals, splits them across local-midnight boundaries (overnight shifts), and applies the restaurant's OB windows + optional overtime as supplements **on top of** base pay (no double-counting). Produces a transparent per-line breakdown (base, each OB window, overtime) that links back to the exact stamp ids — §8.2 spårbarhet. 11 unit tests.
- ✅ **OB-rule presets** (`src/lib/payroll/rules.ts`, §13) — owner picks a preset template ("Inget OB-tillägg" / "Kväll & helg"), not free-form entry. Stored **versioned** as an `OBRuleSet` row so an approved draft traces to exactly which rules applied.
- ✅ **AI presentation layer** (`src/lib/ai/payroll-note.ts`) — Claude **Haiku** (`claude-haiku-4-5`, the model the spec specifies §8) writes a short Swedish summary of the *already-computed* draft. The AI only summarises/presents — it computes no number and **never writes to the DB** (build rule #6). Degrades gracefully: with no `ANTHROPIC_API_KEY` it returns a deterministic Swedish note, so the flow runs with zero AI keys. System prompt is prompt-cached (§8.3). 3 tests.
- ✅ **suggest → confirm → write** (§8, hard rule) — `POST /api/economy/payroll/preview` computes the draft + note and **persists nothing**; `POST .../approve` is the only writer and runs only behind the owner's explicit click, **recomputing server-side** (never trusts client numbers) and writing `PayrollPeriodSummary` rows with the line breakdown. Members with an **unreviewed deviation** (§6.3) or **no rate** are held back and named, never folded in silently. `payroll:manage` (FULL + admin), verified unauth → 401.
- ✅ **Rates + OB config** — `Membership.hourlyRate` added (nullable `Decimal`; migration applied to the live Neon DB 2026-06-27). `GET/POST /api/economy/payroll/rate` (per-employee rate) and `/ruleset` (OB preset) under settings:manage + FULL.
- ✅ **UI** — new "Löneutkast" tab in `EconomyView`: Generate (AI) → review cards with the transparent breakdown + AI note (AI/fallback badge) + missing-rate/unreviewed flags → Approve & save (confirm-card flow); disclaimer that it's a draft, not a binding payslip. Settings tab gains the OB-preset picker + per-employee rate inputs. This fills the mockup's OB/gross columns that §6.3 deferred.
- ✅ i18n — full `economy.draft` / `rates` / `obRules` catalogs (sv + en). **66 tests green**; production build passes; all 4 payroll routes registered.
- ✅ §8.1 (NL → schedule changes) — the other half of §8; see the "AI scheduling" section below (built in this session, after this line was originally written).
- ✅ **Editable OB-rule builder, replacing the fixed 2-preset dropdown.** No schema change — `OBRuleSet.rules` was already arbitrary versioned JSON. `src/lib/payroll/rules.ts` gains `parseCustomRuleSet` (server-side validation: name required, windows need a unique day set, `0 ≤ start < end ≤ 1440`, percent capped at 500%, optional overtime threshold ≤168h); `POST /api/economy/payroll/ruleset` now accepts `{ custom: {...} }` alongside the existing `{ presetId }`, both still versioned (never overwrites history — an approved draft still traces to exactly the rules live at the time). `ObRulesSection` (`EconomyView.tsx`) gets an "Anpassa egna regler" builder: add/remove OB windows with a day picker + time range + uplift %, plus an overtime threshold/percent toggle. 11 new unit tests on the validator; **98 tests green**; build/lint clean (no new lint issues — the pre-existing repo-wide lint errors are unrelated to this change).

## AI scheduling (this session, §8.1)

- ✅ **NL → structured proposal** (`src/lib/ai/schedule-assistant.ts`) — Claude Haiku with **forced tool-use** (not free text) so the model always returns a typed shift list, never prose to parse. Context fed to the model is real: active members + their tags, and the previous week's actual shifts (so "samma som förra veckan" has something concrete to copy from), per spec §8.1 step 2.
- ✅ **Ambiguity surfaces, never hides** — each proposed row carries `ambiguous`/`note`; a member name that doesn't match anyone in the restaurant is flagged (never silently dropped or fuzzy-guessed) via `resolveMembers`. Unit-tested.
- ✅ **suggest → confirm → write** (§8, hard rule) — `POST /api/schedule/ai/preview` computes the proposal and **persists nothing**; `POST /api/schedule/ai/approve` is the only writer, runs only behind the owner's explicit click, and **re-validates every row server-side** (real member of this restaurant, valid date/time) rather than trusting the client. `ai:schedule` (FULL + admin).
- ✅ **No fabricated fallback** — unlike §8.2's deterministic numbers, there's no honest way to "guess" a schedule interpretation without a model, so with no `ANTHROPIC_API_KEY` the preview route returns 503 `ai_unavailable` and the UI says so plainly, rather than inventing a fake parse.
- ✅ **UI** — `/app/schedule` gets an "AI-schemaläggning" entry next to "Nytt pass" (owner/co-owner, FULL tier only): free-text box → review card listing every proposed shift **by name/date/time** (not a summary sentence, per §8.1 design requirement) → "Ändra manuellt" makes the list inline-editable → "Godkänn" (writes) / "Avbryt" (discards). Ambiguous rows are visually flagged with the model's own note.
- ✅ i18n — `schedule.ai` catalog (sv + en). **69 tests green**; production build passes; both AI routes registered.

## AI fair use (this session, §8.3)

- ✅ **Soft trial cap** (`src/lib/ai/fair-use.ts`) — `recordAiCall()` increments `Restaurant.trialAiCallCount` only while `subscriptionStatus = TRIALING` (paid usage is unmetered) and only counts a real model call (never the no-key fallback path, since that has no AI cost). Crossing **50 calls** sets `trialAiFlaggedAt` once and logs internally — **never** a hard block, exactly per spec ("flaggas internt … INGEN hård spärr"). Best-effort: a tracking failure never breaks the AI feature itself.
- ✅ Wired into both AI call sites — payroll preview (only when `payrollNote` actually used Haiku, i.e. `source === "ai"`) and schedule preview (after a successful `proposeSchedule`, which throws before any count if no key is configured).
- ✅ Migration `trial_ai_fair_use_cap` applied to the live Neon DB (additive nullable/defaulted columns).
- ✅ Build passes; existing 69 tests still green (this is a thin DB-backed counter, consistent with the codebase's pattern of not mocking Prisma in unit tests — verified instead by build + code review).

## PWA (this session, §14.3)

- ✅ **Manifest** (`src/app/manifest.ts`, Next's metadata-file convention) — served at `/manifest.webmanifest`, `start_url: /app`, `display: standalone`, brand theme/background colors, 192/512 + maskable icons. Verified: fetches 200 with the right shape.
- ✅ **Icons** (`public/icons/`) — generated programmatically (no design tool/new dependency) from the same rounded-diamond mark as `Logo.tsx`, in the brand teal/cream palette; plain + maskable variants at 192/512.
- ✅ **App-wide install + offline shell** — `src/components/PwaRegister.tsx` registers the existing clock-in service worker (`public/clock-sw.js`, §6.2) from the root layout, so every route gets the install prompt and offline shell, not just the kiosk. **Privacy fix made in the same change:** the SW's cache key is the URL only, with no notion of "whose session" — so `/app/*` (authenticated, per-user HTML) is now explicitly excluded from caching to avoid one user's page being served to the next session on a shared device; only public pages (`/`, `/login`, `/clock/*`, static assets) are cached. Verified live: manifest linked, SW registered at root scope, icons 200, cache contains only public paths after visiting `/` and `/login`.
- ✅ `appleWebApp` + `theme-color` viewport metadata for iOS/Android install chrome.
- ✅ Build + lint + 69 tests still green.
- ⬜ Real-device install/offline verification (Android/iOS home-screen add, full cold-start offline boot) — needs a deployed HTTPS origin, same gap noted in §6.2/§5.

## Billing (this session, §12)

- ✅ **Signup flow** (`/signup`, `src/components/auth/SignupForm.tsx`, `/api/auth/signup/{request-code,verify}`) — discovered this was a real gap, not just unbuilt billing: the landing page already linked `/signup?plan=bas|full` but nothing created a `Restaurant` yet. New `CodePurpose.SIGNUP` OTP step (mirrors login/invite-accept) verifies the founding owner's contact before creating Restaurant + User + Membership(`role: OWNER, isBillingOwner: true`) in one transaction — the soft qualification §12.1 step 4 asks for (name + restaurant name/orgnr + verified contact). Accounts stay person-centric (§3.1): an existing User signing up a second restaurant just gets a second Membership.
- ✅ **Trial unlocks the full package regardless of chosen tier** (§12.1 step 3) — `getAccessContext` in `src/lib/guard.ts` now computes an *effective* tier: `FULL` while `subscriptionStatus = TRIALING`, the row's real `tier` otherwise. One change point fixes every page/route that calls it; previously a Bas-tier signup would have wrongly stayed locked out of clock-in/economy/AI during its own trial.
- ✅ **Frozen-account gate** (§12.1 step 6: "fryses kontot, data bevaras, inloggning blockeras") — `getAccessContext` returns null (no access) for `FROZEN`, same as no membership; `/app` checks subscription status first and routes to `/app/frozen` instead of looping. The frozen page is deliberately *not* gated through `getAccessContext` — it's the one page that must stay reachable to pay your way out.
- ✅ **Trial lifecycle cron** (`src/lib/billing.ts`, `/api/cron/billing`, `vercel.json`) — day-25–28 reminder e-mail to the billing owner (sent at most once, `trialReminderSentAt`), day-30 freeze **only if no Stripe subscription is attached** (never an automatic charge — spec is explicit: "INGEN automatisk debitering av ett kort kunden glömt"). Authenticated via `CRON_SECRET` bearer header, the documented Vercel Cron pattern; verified unauthenticated → 401.
- ✅ **Stripe checkout + portal + webhook** (`src/lib/stripe.ts`, `/api/billing/{checkout,portal,webhook}`) — hosted Checkout Session (monthly/yearly), Customer Portal for managing/canceling, and a signature-verified webhook that's the *only* writer of payment outcome (checkout/portal merely start a Stripe flow — suggest → confirm → write applies to money too). Billing-owner only (`requireBillingOwner` in guard.ts), the one documented exception where OWNER/CO_OWNER aren't interchangeable (§3.2) — checked directly on `Membership.isBillingOwner`, not a new permission tree (CLAUDE.md rule #4).
- ✅ **Setup script** (`scripts/stripe-setup.mjs`) — creates the Bas/Fullt Products + monthly/yearly Prices via the Stripe API (yearly = 10× monthly, ~2 months free, the §12.3 retention discount) so the Price ids in `.env` always match what's actually configured; not hand-typed in the dashboard. Not yet run — needs your `STRIPE_SECRET_KEY` in `.env` first.
- ✅ **UI** — `/app/billing` (trial countdown / active badge, monthly/yearly checkout buttons, manage-subscription link once a card exists) and `/app/frozen` (the escape hatch — same checkout buttons, plus an "ask the owner" message for non-billing-owner staff). Nav entry added next to Members/Economy for owners/co-owners.
- ✅ Migration `billing_signup_trial` (CodePurpose.SIGNUP, Restaurant.trialReminderSentAt) applied to the live Neon DB. Build + lint + 69 tests green. **Verified end-to-end against the real DB** with a throwaway test restaurant (created, then deleted): signup → session → `/app/schedule`; flipped to FROZEN → `/app` correctly redirects to `/app/frozen`, direct `/app/schedule` hit bounces the same way; checkout → 503 `billing_unavailable` (no Stripe key yet, degrades cleanly instead of fabricating a link); portal → 400 `no_customer`; cron → 401 unauthenticated.
- ✅ **Stripe fully wired** — `STRIPE_SECRET_KEY` (test mode) set; `scripts/stripe-setup.mjs` run, Bas/Fullt monthly+yearly Prices created and IDs in `.env`; webhook endpoint added in the Stripe dashboard pointed at `/api/billing/webhook` with its signing secret in Vercel; `CRON_SECRET` generated and set in both `.env` and Vercel. Live-verified: `/api/billing/checkout` returns a real `checkout.stripe.com` URL and persists `stripeCustomerId` on the restaurant (tested + cleaned up against the real DB and a real test-mode Stripe customer).
- ✅ **Real Checkout completion confirmed in production** by the owner: full Stripe flow (test card, redirect back, webhook flipping `TRIALING`→`ACTIVE`) worked end-to-end on the deployed app.
- ✅ **Post-launch fix — double-purchase bug**: the owner caught that `/app/billing` kept showing "betala månadsvis/årsvis" even after `subscriptionStatus` was `ACTIVE`, which would have created a second Stripe subscription instead of changing the existing one. `BillingView.tsx` now shows only "Hantera abonnemang" (Stripe portal — upgrade/downgrade/cancel) once `ACTIVE`/`PAST_DUE`; `/api/billing/checkout` also rejects a second checkout with 409 server-side, not just a hidden button.
- ✅ **Post-launch fix — login friction**: `/login` always rendered the OTP form even with a valid 30-day session cookie already present, so landing back on it while still logged in forced a needless re-auth. It now checks for a session first and redirects straight to `/app`. The owner confirmed this — not a new feature, fixed a bug in the existing "remember me" session.
- ⬜ Referral ("bjud in en annan restaurang") and the move-restaurant-on-churn flow (§12.4) — not started, lower priority per spec ("liten funktion").
- ✅ **Remembered-device fast login, no PIN** — `src/lib/device-token.ts` (90-day signed cookie, edge-safe) is set by `rememberDevice()` alongside every OTP success path (`/api/auth/verify` login, `/api/auth/signup/verify`, `/api/join/[token]/accept` — owner and personal alike). `src/proxy.ts` and `/login` (`src/app/login/page.tsx`) both fall back to the device cookie when there's no live session, silently re-minting one (no OTP, no PIN) rather than bouncing to the login form. Explicit logout (`destroySession`) clears the device cookie too, so "log out" can't be defeated by an immediate silent re-login. Build + lint + 87 tests green.
- ✅ **Polish the expired-session landing (lighter than originally scoped).** The original framing ("redirects to the public landing page") is now mostly moot: `/api/auth/verify`'s sibling work this session (remembered-device login, above) means hitting `/app` with a dead session usually silently re-mints one and never shows a form at all. For the remaining case — the device cookie itself is gone (90+ days, or an explicit prior logout) — `proxy.ts` already lands on the minimal branded `/login` page, not the marketing site. What was missing was *framing*: `LoginForm.tsx` now shows "Du har blivit utloggad" / "you've been signed out" copy whenever it's reached via the proxy's `?next=` bounce (an involuntary redirect) instead of the generic first-time-login copy, so it reads as "your session ended" rather than dropping back to a blank login prompt. The blurred app-shell/pop-over treatment was explicitly "not essential" and is skipped — the device-cookie fix removes most of the friction it was meant to soften.

## Legal / GDPR (this session, §13)

- ✅ **Privacy policy + DPA pages** — `/privacy` and `/dpa` (`src/components/legal/LegalDoc.tsx`, content in the i18n catalogs read structurally via `m` so even long-form legal prose obeys rule #1). Privacy policy states the **legal basis is fullgörande av anställningsavtal / rättslig förpliktelse, not consent** (§13: consent is weak in an employer–employee relationship), explains WebAuthn keys aren't biometric PII, and covers retention + data-subject rights. DPA frames Skifta as **personuppgiftsbiträde** (processor) for the restaurant (controller), Art. 28 GDPR. Linked from the landing footer.
- ✅ **DPA built into onboarding** (§13: "en standard-DPA byggs in i onboarding") — the signup account-creation step now shows an acceptance line linking the DPA + privacy policy; creating the account is accepting them.
- ✅ **Right-to-erasure / "anställd slutar" flow** (`src/lib/gdpr.ts` → `removeMember`, `POST /api/members/[userId]/remove`, owner/co-owner only, both tiers) — ends the `Membership`, clears the restaurant-scoped secret (PIN) + competence tags, and **anonymizes the global `User` only when it's their last restaurant** (person-centric, rule #3: a multi-restaurant worker keeps their identity where they still work). **ClockEvent/payroll history is preserved** (append-only, accounting) — it just loses its link to a named person. Refuses to remove the billing owner or yourself. Admin UI: a "Anställda" people list with a confirm-gated erase action (`PeopleAdmin.tsx`).
- ✅ **Automatic retention sweep** (`runRetentionSweep`, folded into the daily billing cron) — anonymizes the PII of people whose employment ended longer ago than each restaurant's `RetentionPolicy.anonymizeAfterMonths`; a restaurant with no policy set never triggers anonymization (conservative). Pure selection logic (`eligibleForAnonymization`, `monthsBetween`) is unit-tested (9 tests), person-centric by construction.
- ✅ Schema: `User.anonymizedAt` (additive, nullable). Migration `gdpr_erasure_retention` **written + client regenerated, but NOT applied to the live Neon DB** — the auto-mode classifier (correctly) blocked a production migration on the shared owner-PII DB without explicit owner go-ahead. Run `npx prisma migrate deploy` at deploy time. Build + lint clean, **87 tests green**, `/privacy` + `/dpa` render verified in the preview (200, correct content, no console errors).
- ⬜ Statutory **hard-deletion** of `ClockEvent` rows after the full `clockDataRetentionMonths` period — deliberately deferred (irreversible, tensions with append-only rule #2, and the spec says "verifiera aktuell lagstadgad period vid implementation" / consult a lawyer). Anonymization already satisfies erasure; once the user is anonymized the kept rows are nameless timestamps.

## Spec section → status

| § | Area | Status | Notes |
|---|------|--------|-------|
| 1–2 | Vision, design principles | ✅ | Encoded as constraints throughout; see CLAUDE.md. |
| 3 | Roles & permissions | 🟡 | Modeled + enforcement layer ✅ (`authz` + `guard`, negative tests). Member-management UI ⬜. |
| 4 | Invite system | ✅ | Token gen/accept/revoke/bulk-paste ✅, verified end-to-end. CSV file upload ✅ (paste box or upload a CSV; tolerant of Excel's BOM/`;`/header quirks). Binary `.xlsx` 🚫 (export to CSV first). |
| 5 | Clock-in identity (WebAuthn/PIN) | 🟡 | PIN method, QR/kiosk + stamp flow, tier gate ✅ (verified). WebAuthn reg/discoverable-auth built (needs real-device verification). Device re-registration (self-service OTP) + owner-initiated reset ✅ (build/lint clean, not live-exercised this session). <3s lunch-rush field test ⬜. Geofencing 🚫. |
| 6.1 | Shifts (week view, open shifts, swaps, availability) | ✅ | Week view, CRUD, open-shift fill (both modes, server-enforced), tag matching, full swap lifecycle, availability ✅, verified end-to-end. Escalation cron (`/api/cron/shifts`, `*/15`), notifications (assign/change/swap-needs-reply/escalated via real SMS/email + in-app feed), and double-booking prevention (409 on overlap, all assign paths) ✅. **Multi-slot shifts, bulk/recurring creation, in-app notification center** ✅ (migration written, not yet applied — see Shifts section above). |
| 6.2 | Clock-in section + offline + tolerance window | 🟡 | Stamp (append-only, idempotent), own history + hours, graded tolerance/deviations, IndexedDB queue + SW shell cache, weekly deviation digest ✅ (verified/build-clean). Mobile clock-IN now QR-gated (camera scan required, clock-OUT unchanged) ✅; "vi känner inte igen dig" mis-attribution bug fixed ✅. Cold-start PWA offline boot ⬜ (needs a real deploy). |
| 6.3 | Economy/admin (summary, deviations, export) | ✅ | Owner UI (4 tabs), hour summary, deviation review (append-only adjust), export gate (unreviewed → blocked), Fortnox/Visma/CSV formats + save-default, CUSTOM template upload (own column mapping), export-all-my-data, realtime overview, weekly deviation digest ✅. OB/gross → §8.2 (also ✅, see below). |
| 7 | Competence tags | ✅ | `Tag`/`EmployeeTag` + admin UI (create/delete tags, assign per member) + qualification matching enforced in shift claim/interest/swap-accept. |
| 8.1 | NL → schedule changes (AI) | ✅ | Haiku forced tool-use → structured proposal, confirm-card UI (exact list, editable, ambiguity shown), suggest→confirm→write, server-side re-validation on approve. No-key → explicit 503, no fake fallback. |
| 8.2 | Payroll draft (AI) | 🟡 | Deterministic OB/overtime engine (traceable line items), Haiku presentation note (graceful no-key fallback), suggest→confirm→write, rates + OB presets, draft UI ✅. Real agreement rates + CUSTOM template ⬜. |
| 8.3 | AI cost / fair-use | ✅ | Prompt caching on the note's system prompt ✅. Soft ~50-call trial cap (flag, not block) ✅. |
| 9 | Data model | ✅ | See `prisma/schema.prisma`. |
| 10 | Discreet complexity (UI) | ✅ (principle) | Enforced as a build rule; re-checked per view. |
| 11 | i18n + mobile-first + landing | ✅ | i18n + landing done. Mobile-first app shells ⬜. |
| 12 | Business model (trial, tiers, pricing) | 🟡 | Pricing UI ✅. Tier middleware + **negative tests** ✅. Signup + 30-day trial (full package unlocked regardless of tier) + soft-qualification + reminder/freeze cron + Stripe checkout/portal/webhook ✅, verified live end-to-end in production by the owner. Double-purchase + login-friction bugs found and fixed same-session ✅. Referral + move-restaurant flows ⬜. Passkey login is a flagged future idea, not started. |
| 13 | Legal / GDPR / retention | ✅ | `OBRuleSet` (versioned), `RetentionPolicy` modeled. Privacy policy (`/privacy`) + DPA (`/dpa`) pages (i18n, legal basis = anställningsavtal/rättslig förpliktelse not consent), DPA accepted at signup, erasure flow (`removeMember`, person-centric-safe, preserves append-only ClockEvent), retention sweep (auto-anonymize long-departed staff) ✅. Statutory ClockEvent hard-disposal after the full period deferred (lawyer-gated, tensions with append-only rule #2). |
| 14 | Hosting / VCS / PWA path | 🟡 | Git repo ✅. PWA manifest+SW ✅ (installable, app-wide offline shell). Vercel deploy ⬜ (native 🚫). |
| 15 | Acceptance criteria | ⬜ | Test suite ⬜ (esp. tier 403 negative tests, offline-no-loss, append-only). |
| 16 | Payroll-admin workflow reference | n/a | Guides prioritization; steps 1–4 are the value target. |
| 17 | Out of MVP scope | 🚫 | BankID, own tax calc, OCR, geofencing, native app, per-tier codebases, languages beyond SV. |

## Suggested next milestones (in order)

1. ✅ **Auth spine** — OTP login, session, restaurant picker (§3.3), DB provisioned and verified end-to-end.
2. ✅ **Tier/role middleware + negative tests** (§12.2) — single authorization layer; Bas → 403 proven in tests.
3. ✅ **Invite system** (§4) — single-use tokens, OTP accept, revoke, bulk-paste invite, **CSV file upload** (Excel-tolerant parser, reuses the validated bulk endpoint), admin UI. Complete.
4. ✅ **Shifts section** (§6.1) — week view, open shifts (both fill modes, server-enforced), swap flow with tag matching (§7), availability, tags admin, escalation cron, notifications (real SMS/email), double-booking prevention. Complete.
5. ✅ **Clock-in** (§5, §6.2) — PIN + WebAuthn identity, QR/kiosk, append-only idempotent stamps, graded tolerance/deviations, own history + hours, offline queue + SW, admin QR/tolerance setup, lost/new-device self-service re-registration + owner-initiated reset. ⬜ Real-device WebAuthn + <3s field test, cold-start PWA offline remain (need a deployed HTTPS origin).
6. ✅ **Economy/admin** (§6.3) — period summary + per-employee hours, deviation review (append-only adjust), export gate (unreviewed never silent), Fortnox/Visma/CSV formats + save-default, "export all my data". ⬜ OB/gross (→§8.2), CUSTOM template upload, deviation digest remain.
7. ✅ **AI** (§8) — payroll draft (§8.2) ✅, NL→schedule (§8.1) ✅, soft fair-use trial cap (§8.3) ✅: deterministic OB/gross engine + Haiku note for payroll, Haiku forced tool-use → confirm-card for scheduling, both suggest→confirm→write, both AI call sites counted against a 50-call/trial soft cap that flags-not-blocks.
8. ✅ **PWA** (§14.3) — manifest + app-wide service worker registration (reuses the offline-queue SW from §6.2), installable home-screen icon, themed standalone display.
9. ✅ **Billing** (§12) — signup flow, 30-day trial (full package unlocked regardless of chosen tier), frozen-account gate, reminder/freeze cron, Stripe checkout/portal/webhook all built, keys configured (test mode), checkout verified live in production by the owner (including a real webhook-driven `TRIALING`→`ACTIVE` flip), and two post-launch bugs the owner caught (double-purchase, login friction) fixed same-session.
10. **Passkey/Face ID login** (owner-flagged, not started) — a faster repeat-login path beyond the OTP code, likely adapting the WebAuthn infra from §5 clock-in to the login identity surface. Needs its own design pass before building.

## Open items to confirm with the owner

- **Trial length:** the mockups say "14-day free trial"; spec §12.1 says **30 days** (deliberate, to span a payroll cycle). The build follows the spec (30). Flag if the mockups should win instead.
- **Visual preview tooling:** the in-editor preview/screenshot tool is bound to the *other* project in this workspace (the primary working dir), so live screenshots of Skifta aren't available through it. Verification is via production build + rendered-HTML checks; a Vercel deploy would give a shareable live URL.
- **"Always logged out" bug — need the owner to check two things:** (1) which URL they've been visiting after each deploy — the stable production domain, or a fresh per-deployment Vercel URL (the latter wouldn't carry the session cookie, by design); (2) in the Vercel dashboard, whether `AUTH_SECRET` is set once for the **Production** environment and has never been edited/regenerated (it signs both the login session and every clock-kiosk QR token, so a change invalidates everything at once). See the Auth section above for the full reasoning. **This is also the most likely cause of the separate "vi känner inte igen dig" report** — same secret, same QR tokens; re-confirming this fixes both.
- **Pending migration `multi_slot_shifts_and_notifications`** — written, validated, `prisma generate`'d, **not applied to the live Neon DB** (it backfills 5 existing `assignedUserId` rows into `ShiftAssignment` before dropping the column — real production data, so this needs an explicit go-ahead before `npx prisma migrate deploy`, same rule as the two migrations already pending from earlier sessions).
- **QR-scanning real-device check** — `BarcodeDetector` in-page scanning (Android/Chrome) for mobile clock-in is built but unverified on a real device (same gap as WebAuthn generally — needs a live camera + HTTPS origin). The iPhone/Safari fallback (use the phone's own camera app) needs no code, just confirming the printed QR still opens `/clock/<token>` correctly from the native camera.
